import { createBrowserHistory } from 'history';

// Create history object
// We can access it eveywhere inside the project
// Does not depend on BrowserRouter to maintain history object
export default createBrowserHistory();
